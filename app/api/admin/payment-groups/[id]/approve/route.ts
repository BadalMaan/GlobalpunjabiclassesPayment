import {NextResponse} from "next/server";import {getAdminSession} from "@/lib/auth";import {supabaseAdmin} from "@/lib/supabase";import {markGroupPaid} from "@/lib/payment";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const session=await getAdminSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const {data:g}=await supabaseAdmin.from("payment_groups").select("*").eq("id",id).single();if(!g)return NextResponse.json({error:"Not found"},{status:404});try{await markGroupPaid(g.id,{method:(g.payment_method||"BANK_TRANSFER") as any,transactionId:g.payment_reference||null,actor:String(session.email||"admin") as string});return NextResponse.json({ok:true});}catch(e:any){return NextResponse.json({error:e?.message||"Could not verify"},{status:400});}}
